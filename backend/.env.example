SERVER_PORT=8080

# Database connection (option 1: full URL)
DATABASE_URL=postgresql://user:password@localhost:5432/menu_tree?sslmode=require

# Database connection (option 2: individual params)
# DATABASE_HOST=localhost
# DATABASE_PORT=5432
# DATABASE_USER=postgres
# DATABASE_PASSWORD=postgres
# DATABASE_NAME=menu_tree
# DATABASE_SSLMODE=disable
